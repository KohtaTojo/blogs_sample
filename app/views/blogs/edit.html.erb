<% if @blog.errors.any? %>
  <h2><%= @blog.errors.count %>件のエラーがあります。</h2>
  <ul>
    <% @blog.errors.full_messages.each do |msg| %>
    <li>
      <%= msg %>
    </li>
    <% end %>
  </ul>
<%end %>
<h1> update blog</h1>
<%= form_for @blog do |f| %>
  <%= f.label :content, "内容" %>
  <%= f.text_field :content %>
  <%= f.submit "Send" %>
<% end %>
